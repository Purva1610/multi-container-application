version: '3'
services:
  web:
    build: .
    ports:
      - "5000:5000"
    environment:
      FLASK_ENV: development
      OPENWEATHER_API_KEY: "${OPENWEATHER_API_KEY}"
    volumes:
      - .:/app
    restart: unless-stopped

# db service removed; app now uses OpenWeather API only
